apiVersion: v1
kind: Service
metadata:
    name: order-service
    namespace: ecommerce
    labels:
        app.kubernetes.io/name: order-service
        app.kubernetes.io/part-of: ecommerce
        observability: enabled
spec:
    type: ClusterIP
    selector:
        app.kubernetes.io/name: order-service
    ports:
        - name: http
          port: 8083
          targetPort: 8083
